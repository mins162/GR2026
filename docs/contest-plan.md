# 반도체설계 경진대회 준비 계획

- 최종 갱신 : **2026-08-26**
- 대회 : 2026 한국 대학생 반도체 설계 경진대회 ((사)반도체공학회)
- **접수 마감 : 2026-09-18 (금) 14:00** — 참가신청서 + **연구결과 설계보고서** 동시 제출 (`secretary@theise.org`)
- 이후 일정 : 예선 결과 10-02 → 본선 발표 10-09 → 최종 발표 10-23 → 시상식 11-26
- 시상 : 대상 1 / 금상 1 / 은상 2 / 동상 3 = **총 7팀**
- 팀 : 학부생 4명 + 지도교수 1명 = 5명 (**규정 상한 정확히 채움**, 추가 불가)
- 배점 : **창의성 30% · 난이도/우수성 30% · 완성도 40%**

---

## 0. 전략 요약

> **기술 수준은 상위권. 남은 변수는 그걸 12페이지 안에 담아내느냐다.**
> 남은 3주는 **추가 최적화가 아니라 압축 + 시각화 + 서사**에 쓴다.

근거 :

- 현재 결과 (**intern 서버 / RTX 3060**, [rtx3060-ab.md](rtx3060-ab.md) — 60런 A/B 매트릭스) :
  - `mempool_group` 92.47s → **28.28s (3.27×)** — 보고서 헤드라인은 **이 숫자**
  - `nvdla` 2.63× · `bsg_chip` 2.13× · `mempool_tile_rank` 1.40×
  - 절감의 **73%가 incremental vcost/presum 하나**
  - 품질 : 프로그램 report 기준 −0.035% ~ +0.012% (노이즈 수준), 전 런 open 0 · incompleted 0
  - `mempool_cluster_ranking` — 3060에서 opt가 GPU-FLUTE scratch OOM (12GB). TITAN 결과 병기
- 28.3s를 26s로 더 줄여도 심사 점수는 안 오른다. 반면 라우팅 이미지 한 장은 확실히 오른다.
- 지금 `docs/`는 **표 위주, 그림 0장**. 학부 심사에서 표만 있는 보고서는 안 읽힌다.

### 리스크

| # | 리스크 | 상태 / 대응 |
| --- | --- | --- |
| 1 | ~~주제 적합성~~ | **해소 (2026-08-26)** — 사무국 문의에 **운영위원회 논의 결과 "제출 가능한 주제"** 회신. 메일 원문 보관할 것. → 보고서에서 적합성을 **변명조로 설득하지 말 것** (스스로 주변부라고 광고하는 꼴). "설계 반복 2배" 프레이밍(§3-1)은 유지하되 목적이 바뀜 — 방어가 아니라 **수치를 설계자 언어로 번역** |
| 2 | **12페이지 초과** | **최대 리스크.** 양식이 12p 상한 + 3장 고정 구조를 강제한다. 지금 `docs/` 분량은 20p급 → **버릴 것을 정하는 게 이번 작업의 본질**. §2 |
| 3 | **"논문 반증" 역공** | *"논문이 나쁜 게 아니라 너희 구현이 나빴던 것 아니냐"* 에 반박할 자료가 없다. **표현을 낮춘다** — §3-2 |
| 4 | "오픈소스 갖다 쓴 거 아니냐" | **기여 경계표를 먼저** 제시 (§4-1). 숨기면 죽고, 먼저 그으면 강점. 공고의 reference 표기 의무도 이걸로 충족 |
| 5 | 그림 없음 | §1 |

### 배점 대비 자기 평가

| 항목 | 배점 | 예상 | 왜 |
| --- | ---: | ---: | --- |
| **완성도** | 40 | 34~38 | **최강.** LOO×사다리 교차검증, 60런/불일치 0, 재현성 폭 명시, negative result, 한계 자진 명시 |
| 창의성 | 30 | 18~24 | 아이디어는 진짜 좋으나 회로 전공 심사위원에겐 "소프트웨어 최적화"로 읽힐 위험 + 출발점이 오픈소스. **편차 최대** |
| 난이도 | 30 | 18~23 | 규모(3.2M net / 38.7M cell)는 먹히지만 **칩을 안 만들었다**가 걸린다 |

→ 완성도를 다 먹어도 나머지 60점이 구조적 약점이라 **대상권은 아니다.** 현실적 목표는 **수상(7팀) 진입**.
그래서 완성도를 깎아먹는 리스크 2번(분량 초과)이 제일 위험하다 — **최대 강점을 스스로 무너뜨리는 유일한 경로**.

---

## 1. 시각화 — **최우선** (~9/1)

데이터는 전부 이미 있다. 새 실험 없이 만들 수 있는 것들.

### 참고 — `.out` 포맷 (ISPD 2024 GR)

`src/graph.hpp:392` `output_nets()` 기준 :

```
<net name>
(
x0 y0 l0 x1 y1 l1      ← wire segment 또는 via (l0 != l1)
...
)
```

- 좌표 인코딩 : `pos / Y % X` = x, `pos % Y` = y, `pos / X / Y` = layer
- 즉 **파싱만 하면 전부 렌더링 가능**. 별도 계측 불필요.

### 후보 그림 6종

| # | 그림 | 데이터 소스 | 보고서 위치 | 전달 메시지 |
| --- | --- | --- | --- | --- |
| **V1** | **라우팅 결과 오버뷰** — 칩 전체 배선, 레이어별 색 | `test.out` 파싱 | 서론 / 결과 | "얘네 진짜 칩을 라우팅했구나" — 심사위원이 회로 대회에서 기대하는 **그림다운 그림** |
| **V2** | **혼잡도(overflow) 히트맵 — baseline vs 우리 것 나란히** | `.cap`(capacity) + `.out`(demand) | 결과 / 품질 검증 | **품질이 같다는 걸 숫자가 아니라 눈으로**. 표의 −0.035%보다 설득력 큼 |
| **V3** | **CPU+GPU 통합 타임라인 (Gantt)** | NVTX(`src/nvtx_profile.hpp`) + nsys + 호스트 타이머 | 방법 §GPU-FLUTE | **GPU-FLUTE가 CPU 루프 뒤에 완전히 숨는 것** (`tail beyond CPU loop = 0.000s`) — 이건 표로는 절대 전달 안 됨 |
| **V4** | **누적 사다리 스택 막대** — 92.47s → 28.28s | [rtx3060-ab.md](rtx3060-ab.md) §3 | 결과 요약 | 어디서 줄었는지 한눈에. 표 하나를 그림 하나로 |
| **V5** | **batch gen 재설계 도식** — 논문 방식 vs 우리 방식 | `docs/optimizations.md` §4 | 방법 §batch gen | net당 commit **430회 → 2.8회**. 창의성 30% 핵심 그림 |
| **V6** | **batch 커버리지 히스토그램** | `archive/2026-08-23-best-result.md` 커버리지 표 (P50 **1.2%**) | 방법 §incremental | "batch가 grid의 1.2%만 건드리는데 100%를 재계산한다" — **우리 최고 아이디어의 근거 그림** |

### 우선순위 — **본문에는 3장만 넣는다**

12페이지 상한 때문에 6장을 다 넣으면 본문이 죽는다. 지면을 실제로 살 값어치가 있는 것은 셋뿐이다.

| | 채택 | 임팩트 | 난이도 | 비고 |
| --- | :---: | --- | --- | --- |
| V3 통합 타임라인 | **○** | ★★★ | 중 | **표로는 절대 전달 안 되는 유일한 기여.** `nvtx_profile.hpp`·`tools/nsys_summarize.py` 확장 |
| V2 혼잡도 히트맵 | **○** | ★★★ | 중 | 양식 3장의 *"기존 기술들과의 차이점 비교"* 를 눈으로. `.out` 페어 이미 확보됨 |
| V4 스택 막대 | **○** | ★★ | 하 | 누적 사다리(§3 of `rtx3060-ab.md`) → matplotlib. **표를 대체**하므로 지면이 오히려 준다 |
| V1 라우팅 오버뷰 | △ | ★★★ | 하 | 자리가 남으면 서론에 작게. 3.2M net이라 다운샘플 필수 — **시간 태우기 쉬운데 임팩트는 "예쁜 그림" 이상이 아님** |
| V5 batch gen 도식 | △ | ★★ | 하 | 개념도. 넣으면 좋지만 본문 텍스트로 대체 가능 |
| V6 커버리지 히스토그램 | ✕ | ★★ | 하 | "P50 1.2%" 한 줄로 충분. 그림까지 쓸 지면 없음 |

### 주의

- **`mempool_group`은 3.2M net** — V1을 그대로 그리면 렌더링이 안 끝나거나 검은 사각형이 된다. 레이어별 분리 + grid 단위 집계(픽셀당 배선 개수)로 그릴 것
- V2는 baseline과 opt의 **같은 런 페어**에서 뽑아야 함 (`best-result.md` 각주 참고 — `.out`이 덮어써지는 사고 있었음)
- V3는 nsys 런이 별도 config였던 전례 있음 → **어느 런의 그림인지 캡션에 명시**

---

## 2. 보고서 구조 — **양식이 강제한다**

`2026-반도체설계경진대회_참가신청서_0426.pdf` 확인 결과, 자유 목차가 아니다.

- **설계보고서 최대 12페이지**, 맑은고딕 10pt, 작성 지침은 삭제하고 제출
- 목차 3장 **고정** : ①설계 요약(2p 이내) ②구성 및 동작 ③설계 과정 및 실험 결과
- ①은 다시 **창의성 / 난이도 / 완성도** 소절 + **box 안 10줄 요약** — 배점 30/30/40이 그대로 소절 이름. 심사표가 이 3소절을 직접 읽는 구조
- ③에 ***"기존 발표된 논문/특허 기술 사용 시 reference 표기 필수"*** 명문화 → §4-1 기여 경계표의 자리가 이미 확보돼 있다

### 페이지 예산 (합 12p — 초과분은 자른다)

| 장 | 지면 | 내용 | 그림 |
| --- | ---: | --- | --- |
| **1. 설계 요약** | **2.0p** | box 10줄 → 창의성 → 난이도 → 완성도. **배점 100%를 직접 건드리는 유일한 2페이지** | — |
| **2. 구성 및 동작** | **3.5p** | 파이프라인 도식(CPU/GPU 레인 구분) 위에 기여 지점 ①~⑥ 마킹 → 3장에서 번호로만 참조해 **중복 설명 제거**. InstantGR 1.0 구조와 ISPD 2024 벤치마크는 여기서 짧게 | **V3** |
| **3. 설계 과정 및 실험 결과** | **6.5p** | 기여 경계표 → 기여별 `관찰/설계/결과/검증` 4줄 고정 포맷(§4-2 순서) → 앵커표 → 누적 사다리 → 품질 → negative result → 한계 | **V4, V2** |

### 버리는 것 (기존 목차 대비)

- **서론·배경 독립 절** → 2장 앞 3~4문장으로 흡수. 12p에서 서론에 1p 쓰는 건 사치
- **검증 독립 절** → 각 기여의 "검증" 줄 + 3장 말미 한 문단으로 분산
- **부록(재현 절차)** → 통째로 삭제. 12p 안에 부록 자리는 없다. 필요하면 각주로 저장소 링크 한 줄
- **향후 계획** → "한계" 3줄에 병합 (FLT 미구현 / cluster OOM / 호스트 병목)

> 원본 문서(`optimizations.md`, `rtx3060-ab.md`)는 그대로 두고 **보고서만 압축본**으로 쓴다.
> 심사에서 더 물으면 원본을 보여줄 수 있다는 게 오히려 강점.

---

## 3. 서사 — 창의성 30% 공략

### 3-1. 첫 페이지 프레이밍

"라우터를 2배 빠르게 했다"(X) → **"설계 반복을 2배로 만들었다"**(O)

> 칩 하나를 설계할 때 라우팅은 P&R 시간의 상당 부분을 차지하고, 설계 반복 한 번의 대기 시간을 결정한다.
> 우리는 이 단계를 GPU로 가속해 **설계자가 하루에 돌릴 수 있는 반복 횟수를 2배로** 만들었다. 배선 품질 손실은 0이다.

심사위원이 **반도체 설계자의 언어로** 이해할 수 있어야 한다.

### 3-2. 핵심 서사 — "구현해 보고, 측정하고, 다시 설계했다"

대부분의 학부 보고서는 "이거 했고 몇 % 빨라졌습니다" 나열이다. 우리는 다른 걸 갖고 있다 :

> TCAD 2026 저널판이 기여로 내세운 GPU batch generation(§III-D)을 **논문 기술대로 구현한 1차 버전은 CPU보다 3.7배 느렸다** (S2 3.50s → 12.93s).
> 원인을 분석하니, net 하나가 자기 최종 batch 번호만큼 full commit을 반복해 net당 **~430회**였다.
> CPU가 빠른 진짜 이유는 병렬성이 아니라 **실패가 싸기** 때문이었다 (`has_conflict`가 첫 충돌에서 즉시 리턴).
> → 논문의 우선순위 중재(atomicMin)는 살리고 **스케줄링만 버려서**, 실패를 싸게 만드는 window/wavefront 구조로 재설계.
> → net당 warp로 pick 의존 체인을 1/32로 단축.
> → **S2 12.93s → 1.18s.**

#### ⚠ 표현 수위 — 여기가 제일 역공당하기 쉽다

- **쓰지 말 것** : "논문을 반증했다", "SOTA를 이겼다"
  → *"논문 방식이 느린 게 아니라 너희 구현이 나빴던 것 아니냐"* 한 방에 무너진다. net당 430회 commit은 **논문의 결함**이 아니라 **우리가 읽은 대로 구현한 결과**일 수 있고, 반박할 자료가 우리에게 없다
- **쓸 것** : **"§III-D를 우리 하드웨어에서 충실히 구현한 결과 대비, 우리 재설계가 11배 빠르다"**
  - 주장이 약해지는 게 아니라 **방어 가능해지는 것**이다. 수치(12.93 → 1.18s)는 그대로 살아 있다
  - 비교 대상을 "논문"이 아니라 **"우리의 1차 구현"**으로 명시하면 공격면이 사라진다
- 헤드라인은 이 항목이 아니라 **1위 기여(incremental vcost/presum)**에 건다 — 그쪽은 이런 반박 자체가 불가능하다

### 3-3. 두 번째 서사 — 하나의 원리

> 관찰 : batch 하나가 건드리는 grid는 **P50 기준 1.2%**. 그런데 baseline은 매 batch마다 100%를 재계산한다.
> 원리 : GPU 라우터의 병목은 병렬화 부족이 아니라 **"변경분 대비 재계산량의 비대칭"**이다.

- 이 원리로 얻은 것이 전체 절감분의 **84%** — vcost/presum 73.3% + wire-demand commit 10.7% (`mempool_group` LOO 기준)
- 두 기여가 **같은 원리의 두 적용 사례**라는 걸 보고서에서 명시할 것. 따로 나열하면 "최적화 여러 개", 묶으면 **"원리 하나"** 가 된다 — 창의성 30%는 후자에만 붙는다
- 이건 **저널 논문 기여 5개 어디에도 없는 우리 고유 아이디어**다 (`archive/2026-08-25-journal-gap.md` 대조 완료)

### 3-4. 세 번째 서사 — 실패도 결과다

- tree-center : depth를 평균 **23% 줄였는데 runtime은 안 줄었다** → critical path가 지배한다는 걸 밝히고 → leaf peeling으로 호스트 비용을 낮춰 순이득 전환
- batch gen 1차 실패 → 5차까지의 재설계 기록
- 파이프라인 : **하지 않기로 판단한 근거**를 정량으로 제시 (이득 상한 6%, 판정 기준 미달)

→ 학부 보고서에서 **판단 근거를 정량으로 제시하고 "안 한다"를 결론으로 쓰는 팀은 거의 없다.** 완성도 40%에 직결.

### 3-5. 네 번째 서사 — 오버랩은 자원이 다를 때만 공짜다

**보고서에서 채택(FLUTE 오버랩)과 기각(batch gen 파이프라인)을 같은 원리로 묶는다.** 이렇게 하지 않으면 "오버랩이 우리 핵심 기여"와 "파이프라인은 안 했다"(§6)가 서로 모순으로 읽힌다.

> **오버랩의 이득은 겹치는 두 작업이 서로 다른 자원을 쓸 때만 나온다.**
>
> - **FLUTE 오버랩 — CPU 루프 ∥ GPU 커널 (다른 자원)** → 이득 그대로. cluster에서 RSMT −17.4s, GPU wall 9.39s가 CPU 뒤로 통째로 숨음 (`tail beyond CPU loop = 0.000s`). **채택**
> - **batch gen ∥ route — GPU ∥ GPU (같은 SM 경쟁)** → 총합이 `max(gen, route)`로 바닥이 막힘. 실측 gen 12.93s > route 5.73s라 이득이 음수. **기각**
>
> 우리는 두 경우를 **모두 구현·측정**한 뒤 하나를 채택하고 하나를 기각했다.

- 근거 문서 : `archive/2026-08-25-pipeline-plan.md` ("겹치는 두 쪽이 CPU↔GPU면 이득이 크고, GPU↔GPU면 유휴율부터 확인")
- 심사 관점 : "오버랩 했습니다"보다 **"오버랩이 언제 통하는지 알아냈습니다"**가 훨씬 강하다. 기각 판단이 같은 원리에서 나오면 §6이 자동으로 정당화된다

---

---

## 4. 우리 기여

### 4-1. 기여 경계표 (보고서 3장 맨 앞에 그대로)

| 구성 요소 | 출처 | 우리 기여 |
| --- | --- | --- |
| InstantGR 1.0 (기반 라우터) | ICCAD'24 오픈소스 `cuhk-eda/InstantGR @ cfce09f` | 출발점 — 명시 |
| Segment 기반 routing graph | TCAD 2026 §III-B/C | 1.0에 이미 포함 (우리 기여 아님) |
| Node-level parallelism | TCAD 2026 §IV | 1.0에 이미 포함 (우리 기여 아님) |
| GPU-FLUTE | ICCAD'22 논문 | 구현 + **CPU 루프와의 오버랩 파이프라인** |
| GPU batch generation | TCAD 2026 §III-D | 우선순위 중재(atomicMin)는 계승, **스케줄링은 폐기하고 재설계** |
| **incremental vcost / presum** | **없음** | **전적으로 우리 것 — 절감분의 73%** |
| **incremental wire-demand commit** | **없음** | **전적으로 우리 것 — 절감분의 11%** |
| tree-center / leaf peeling | 없음 | 우리 것 (순이득, 기본 on) |
| FLT | TCAD 2026 §V | **미구현 — 한계로 명시** |

- 공고 유의사항 : *기존 발표 논문/특허 기술 사용 시 reference 표기 필수* → 이 표가 그 요구를 정면으로 충족
- 효과 : "갖다 쓴 팀" → **"경계를 스스로 그은 팀"**. 표의 절반이 "우리 기여 아님"이라는 게 나머지 절반의 신뢰를 만든다

### 4-2. 우리 기여 — 큰 순서

**수치 기준 : [rtx3060-ab.md](rtx3060-ab.md) §2 leave-one-out** (RTX 3060, 60런). 이전 TITAN 기준 수치는 폐기.
절감 크기와 독창성을 함께 본 순위 — **보고서 3장도 이 순서로 쓴다.**

| 순위 | 기여 | 마진 `group` | 비중 | 마진 `bsg_chip` | 독창성 | 근거 |
| ---: | --- | ---: | ---: | ---: | :---: | --- |
| **1** | **incremental vcost / presum** | **+47.08s** | **73.3%** | +8.72s | ★★★ | 저널 기여 5개 어디에도 없음 |
| **2** | **incremental wire-demand commit** | **+6.89s** | 10.7% | +1.10s | ★★★ | **1번과 같은 원리의 두 번째 적용** — 묶어서 쓸 것 (§3-3) |
| **3** | **GPU batch generation 재설계** | +4.14s | 6.4% | **−0.02s** | ★★★ | 논문 §III-D 대비 재설계. 창의성 서사 최고값 |
| **4** | **CPU/GPU 오버랩 스케줄링** | +1.90s | 3.0% | +1.01s | ★★★ | ICCAD'22 논문에 없는 우리 스케줄링. **GPU가 CPU 뒤로 완전히 숨음** |
| 5 | GPU-FLUTE **알고리즘 도입** | +5.58s | 8.7% | +1.80s | ☆ | **ICCAD'22 논문 것 — 창의성 주장 금지, reference 필수** |
| 6 | tree-center + leaf peeling | +0.55s | 0.9% | +0.25s | ★★ | 우리 아이디어, 순이득 전환 |

- 1+2 = **절감의 84%가 원리 하나**. 이게 보고서 창의성 절의 뼈대다
- 4번과 5번은 종전에 "GPU-FLUTE" 한 덩어리였던 것을 **분리한 것**이다. 분리 근거 : `loo-flt`(+7.48s)에서 `loo-ovl`(+1.90s)을 빼야 알고리즘 몫(+5.58s)이 남는다. 합쳐 놓으면 **우리 스케줄링까지 통째로 "논문 재구현"으로 읽힌다**
- **순위가 절감 크기 순서와 다른 이유** — 5번은 절감이 3번째로 크지만 알고리즘 자체가 남의 것이라 창의성 점수가 안 나온다. 3번은 절감이 작지만 서사 값어치가 가장 크다. 배점(창의성 30%) 기준으로는 이 순서가 맞다
- ⚠ **3번은 `bsg_chip`에서 이득 0(−0.02s, 런 간 폭 안)** — 숨기지 말고 **"이득 조건을 정량화했다"**로 쓸 것. batch generation이 wall에서 차지하는 비중이 큰 디자인에서만 이득이 난다(`group` base 기준 6.7%). §3-5의 "자원이 다를 때만" 원리와 같은 결에 붙는다

#### 1위 — incremental vcost / presum

- **압도적 1위. 절감 크기도 1위, 독창성도 1위.** 이것 하나가 전체 절감의 **73.3%**
- 관찰 : batch가 건드리는 grid는 P50 **1.2%** (cells) — 그런데 baseline은 매번 100% 재계산
- 구현 : ripup/commit이 `dirty_cells`·`track_dirty` 기록 → `update_vcost_selective`가 dirty + 방향 이웃 1칸만 재계산, presum은 track 단위 skip, dirty > 20%면 full rebuild로 폴백
- 결과 : `update_cost` **−99.1%** (9.98s → 0.09s), `compute_presum` −76.8%
- 검증 : nsys(CUPTI) 재측정으로 호스트 타이머와 3.5% 이내 일치 확인
- 예상보다 잘 나온 이유까지 규명함 : 커버리지는 **읽는 영역(bbox)**, skip 조건은 **직전 batch가 쓴 영역** → dirty stamp ≪ 읽기 영역
- → 그림 없음 (V6은 지면 부족으로 기각, "P50 1.2%" 한 줄로 대체)

#### 2위 — incremental wire-demand commit

- **1번과 같은 원리, 다른 적용처.** 따로 세지 말고 **§3-3에서 1번과 한 묶음으로 서술**할 것
- 결과 : `mempool_group` +6.89s (10.7%) / `bsg_chip` +1.10s (8.5%)
- 이게 있어야 "원리 하나가 절감의 84%"라는 문장이 성립한다

#### 3위 — GPU batch generation 재설계

- **창의성 30%의 핵심.** 절감 크기는 3위지만 서사 값어치는 1위
- 논문 기술대로 1차 구현 → **CPU보다 3.7배 느림** (S2 3.50s → 12.93s). ⚠ 표현 수위는 §3-2 참조
- 원인 규명 : net당 full commit ~430회. CPU가 빠른 건 병렬성이 아니라 **실패가 싸서**
- 재설계 : 논문의 atomicMin 우선순위 중재는 유지, **스케줄링만 폐기** → window/wavefront
- 3차 : `thrust::copy_if`의 `cudaMalloc/cudaFree`가 라운드당 4.9ms를 먹는 걸 찾아 호스트 compaction으로 제거
- 4차 : net당 스레드 1개 → **net당 warp** (pick 의존 체인 1/32, `__ballot_sync`)
- 결과 : S2 **12.93s → 1.18s**, batch 수는 CPU first-fit과 사실상 동일 (품질 무손실)
- 검증 : 호스트 시뮬레이션(60k net) + 실기 `VALIDATE=1` conflict-free
- ⚠ **`bsg_chip`에서는 이득 0** — 디자인 의존성을 정량화한 것으로 서술
- → 그림 **V5** (자리가 남으면)

#### 4위 — CPU/GPU 오버랩 스케줄링

- **ICCAD'22 논문에 없는 우리 스케줄링.** 종전에 GPU-FLUTE 덩어리에 묻혀 있던 것을 분리
- 구조 : degree ≥ 10 net은 GPU 커널로, degree < 10 net은 CPU 루프로 **동시에** 처리
- 결과 : S1 RSMT `group` 7.23s → **5.16s**, `bsg_chip` 2.71s → **1.61s**. GPU wall 2.20s가 CPU 루프 뒤로 **완전히 숨음** — `tail beyond CPU loop = 0.000s`
  (`mempool_cluster_ranking`, TITAN : RSMT 38.09s → 20.72s, −64.6%)
- **원리로 승격시킬 것 (§3-5) : 오버랩은 자원이 다를 때만 공짜다**
- → 그림 **V3**. 표로는 절대 전달 안 되는 유일한 기여 — 간트 차트로 GPU 레인이 CPU 레인 밑에 숨는 모습

#### 5위 — GPU-FLUTE 알고리즘 도입

- **알고리즘 자체는 ICCAD'22 논문 것.** 절감(+5.58s / +1.80s)은 크지만 **창의성으로 주장하면 안 되고 reference 필수**
- 우리 것으로 주장 가능한 부분 하나 : **break score O(1) precompute** — 원래의 storage-free 재계산은 candidate당 O(d²)라 2153-pin net에서 ~100s로 붕괴. precompute로 해결하면서 **스코어가 bit-identical**(트리 동일)임을 증명
- 스케줄링(오버랩)은 4위 항목으로 분리했음

#### 6위 — tree-center + leaf peeling

- 절감은 작지만 **"실패도 결과다" 서사의 주인공**
- 이중 BFS tree-center로 DAG depth 평균 **−23%** (max 45 → 28) → 그런데 **runtime은 안 줄어듦**
- 원인 규명 : 평균 depth가 아니라 **critical path**가 지배 + 호스트 측 center-find 비용이 이득을 상쇄
- 해결 : 이중 BFS → **leaf peeling**으로 호스트 비용 절감 → 순이득 전환, **기본 on**
- 현재 프로파일 : S1 eligible 126,006 / center-find 0.145s, S2 eligible 81,968 / 0.058s

---

---

## 5. 일정 · 역할 (4인)

| 기간 | 할 일 | 담당 |
| --- | --- | --- |
| ~9/01 | **시각화 V3·V2·V4** (본문 채택 3장에 집중. V1·V5는 시간 남으면) | 2명 |
| ~9/01 | evaluator 품질 재확인 (`rtx3060-ab.md` §5 — `.out` 페어 보존됨) + `mempool_tile_rank` 1.40× 원인 분석 한 문단 | 1명 |
| ~9/01 | **1장 설계 요약 2페이지 초안** (box 10줄 + 창의성/난이도/완성도) | 1명 |
| 9/02~9/08 | 2·3장 초안 (AI 활용 가능, **서사 구조는 사람이 결정**) | 전원 |
| **9/08** | **⚠ 1차 12p 컷** — 이 시점에 초과분을 잘라낸다. 늦게 자를수록 논리가 끊긴다 | 전원 |
| 9/09~9/10 | 그림 캡션, reference 정리, 참가신청서 작성 | 2명 |
| **9/10** | **⚠ 지도교수께 초안 전달** — 신청서에 **지도교수 서명/인**이 필요하다. 9/17에 부탁하면 늦는다 | — |
| 9/11~9/16 | 교수 피드백 반영, 문장 다듬기, 수치 교차 검증(본문 합이 맞는지) | 전원 |
| 9/17 | 최종 점검 — 12p 이내 확인, 작성 지침 문구 삭제, 맑은고딕 10pt | 전원 |
| **9/18 오전** | 제출 (**14:00 마감 — 오전에 끝낼 것**) | — |

- 기존 계획의 "증분 원리 3번째 적용처"는 **뺐다.** 12p 압축이 최대 리스크인 상황에서 새 실험은 지면을 더 압박할 뿐이고, 심사 점수는 안 오른다 (§6)

---

## 6. 하지 않을 것 (그리고 그 이유를 보고서에 쓸 것)

| 항목 | 안 하는 이유 |
| --- | --- |
| **batch gen ∥ route 파이프라인** | 이득 상한 wall의 **6%** (batch gen이 group 기준 6.4%). 자체 판정 기준 "gen ≤ 3s" 대비 실측 12.93s. 별도 stream+스레드+큐 = 비결정성 리스크가 완성도 40%를 위협. 교과서 기법이라 창의성 점수도 낮음 |
| **FLT 구현** | 논문 알고리즘을 그대로 옮기는 것 → **창의성 점수 0**. 3주에 반쯤 하다 말면 완성도 40%가 통째로 날아감. **한계 절에 "미구현"으로 명시하는 게 이득** |
| 추가 커널 최적화 | 28.3s → 26s는 심사 점수 0점 |

---

## 7. 결정된 것 (2026-08-26)

- [x] **주제 적합성** — 사무국 문의 → **운영위원회 논의 결과 "제출 가능한 주제"** 회신. 메일 원문 보관.
  신청서 *연구 분야* 칸은 **`Digital / EDA (Physical Design — Global Routing)`**.
  문의 메일에 쓴 표현("설계된 회로를 실제 칩으로 구현하는 과정")이 승인받은 표현이므로 보고서 서론에 재사용 가능.
  ⚠ 문의 메일엔 "기계학습 기반"이라 썼지만 실제 작업엔 ML이 없다 — **보고서에서 ML 언급 금지**, 순수 알고리즘/시스템 최적화로 일관되게
- [x] **헤드라인 환경 = RTX 3060으로 통일** (종전 계획의 "TITAN 통일" 권고를 **뒤집음**).
  이유 : 3060 매트릭스가 훨씬 잘 설계됐고(같은 바이너리 토글 / 60런 / LOO×사다리 교차검증), 08-20 표의 오버랩 −0.02s 모순도 여기서 **+1.90s로 이미 해소**됐다.
  TITAN은 `mempool_cluster_ranking` 병기 용도로만 남긴다 ("다른 하드웨어에서도 재현됨")
- [x] **CPU/GPU 오버랩 `mempool_group` 이득 재측정** — 전용 노브 `INSTANTGR_FLUTE_OVERLAP`으로 격리 완료. **−0.02s가 아니라 −1.90s.** 로그가 맞았다 (`optimizations.md` §1 정정 참조)
- [x] **`V2` 히트맵용 baseline/opt `.out` 페어** — `ab_results_0826_143856/*.r1.out` (4개 디자인, 2.2GB) 보존됨

## 8. 열린 질문

- [ ] 이 작업이 연구실 인턴 과제라면, 심사에서 **"본인 기여 범위"**를 물을 수 있음 → 커밋 이력으로 방어 가능한지 확인
- [ ] ⚠ **`mempool_cluster_ranking` opt가 3060에서 OOM** — 가장 큰 디자인(10.6M net)을 최적화 버전으로 못 돌린다. 보고서에서 숨기지 말고 **한계로 명시**하고 TITAN 결과를 병기할 것. 심사에서 "제일 큰 케이스는 왜 없냐"는 반드시 나온다
- [ ] `mempool_tile_rank`가 1.40×로 유독 낮은 이유 — 표에서 눈에 띈다. 최적화가 아니라 **분석**으로 한 문단 채울 것
- [ ] evaluator 품질 재확인 (`rtx3060-ab.md` §5) — 매트릭스는 `SKIP_EVAL=1`로 돌아 프로그램 report만 있다. 보고서 품질표는 evaluator 기준이어야 함
- [ ] 발표(본선 10/09)는 예선 통과 후 대응 — 지금은 보고서에 집중
