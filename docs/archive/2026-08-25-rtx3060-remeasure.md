# 2026-08-25 — intern 서버 (RTX 3060) 재측정

- 환경 : `intern`, **RTX 3060 12GB** (`sm_86`), 단독 사용, 연속 실행
- 이전 기록 : [2026-08-23-best-result.md](2026-08-23-best-result.md) — `gpu-5`, TITAN RTX 24GB (`sm_75`), CPU 공용
- 빌드 : nvcc 12.6.85 (`~/cuda-12.6`, conda) + gcc-toolset-13 (13.3.1), `-arch=sm_86` — 설치 경위는 [my-setup.md](../my-setup.md)
- 코드 : 현재 `main` (tree-center 기본 on — **08-23 기록의 opt는 tree-center 도입 전**)
- 측정 : `run_ab` 스크립트가 아니라 직접 실행. `opt` = 기본 스위치(전부 on), `base` = 같은 바이너리에 6개 스위치 off
  (`GPU_FLUTE` `GPU_BATCH_GEN` `INCREMENTAL_VCOST` `INCREMENTAL_PRESUM` `TREE_CENTER` `GPU_TREE_CENTER` = 0).
  wcost-presum-fusion은 컴파일 타임이라 base에서도 on — 08-23의 `off`와 이 점까지 동일 조건
- 로그/eval : `run/logs-0825-rtx3060/<design>.{opt,base}<n>.{log,eval}` (group은 run1이 무접미사 `.opt.log`/`.base.log`)

> **[대체됨 — 2026-08-26](../rtx3060-ab.md)** : `mempool_group` 배율은 2.68× → **3.27×**로 갱신됐다.
> **상대 비교와 결론은 유효하고 절대값만 낡았다.** 이유는 둘 :
>
> - **base 정의가 달라졌다** — 여기 base는 노브 6개만 껐고 `INCREMENTAL_COMMIT`은 켜진 채였다 (§ 위 측정 항목).
>   새 매트릭스의 base는 7개 전부 off라 더 느리다
> - **여기 `opt`는 `a1bbaaa`(wire demand commit이 배치가 안 쓴 track을 건너뛰게) 직전 상태다.**
>   새 매트릭스에서 그 스위치만 끈 `loo-cmt`가 아래 opt를 거의 그대로 재현한다 —
>   S1 GPU route 5.13 (아래 5.31), S2 host DAG prep 5.00 (5.07), total 34.38 (35.26).
>   그 커밋이 두 구간에서 도합 ~6초를 뺐다

> - `mempool_group` : base 94.52s → opt **35.26s (2.68×)** — 이전 환경 1.99×보다 커짐
> - `bsg_chip` : base 25.03s → opt **13.12s (1.91×)** — **첫 측정** (이전 환경 기록 없음)
> - `mempool_cluster_ranking` : **opt 실행 불가 (GPU-FLUTE scratch OOM, 12GB 한계)** · base 927.45s (§3)
> - 품질 : evaluator 기준 group −0.0013% / bsg_chip −0.035%, 전 런 open 0 · incompleted 0

---

## 0. 비교 유효성

- base run의 **Stage 1 score가 이전 환경 논문 대역과 자릿수까지 동일** (`406,296,750`)
- batch 수 동일 : group base S1 601 / S2 865 (이전과 일치), opt S1 603 / S2 871 (이전 869, +2는 tree-center)
- → 알고리즘 동작은 그대로 재현. 이하 시간 차이는 순수 하드웨어 차이

## 1. `mempool_group` — 3회 평균 (편차 ±1.4% 이내)

- 개별 total : opt 35.21 / 35.77 / 34.81 · base 94.40 / 94.55 / 94.60

| 항목 | TITAN off | 3060 base | 배율 | TITAN opt | 3060 opt | 배율 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| input | 4.61 | 4.35 | 0.94 | 4.65 | 4.37 | 0.94 |
| build CUDA database | 1.66 | (other) | | 1.63 | 1.62 | 0.99 |
| S1: RSMT, CPU FLUTE | 12.50 | 12.57 | 1.01 | 4.93 | 5.08 | 1.03 |
| S1: batch generation | 3.56 | 3.50 | 0.98 | 0.82 | 0.97 | 1.18 |
| S1: DAG build (DFS) | 1.44 | 1.31 | 0.91 | 1.36 | 1.27 | 0.93 |
| **S1: GPU route batches** | **10.75** | **24.36** | **2.27** | **3.47** | **5.31** | **1.53** |
| S2: RSMT/DAG preprocessing | 1.79 | 1.75 | 0.98 | 1.80 | 1.90 | 1.06 |
| S2: batch generation | 3.69 | 3.77 | 1.02 | 1.18 | 1.30 | 1.10 |
| **S2: host DAG prep + upload** | **3.02** | **5.21** | **1.73** | **2.61** | **5.07** | **1.94** |
| **S2: GPU route batches** | **16.61** | **33.97** | **2.05** | **5.73** | **6.18** | **1.08** |
| **total** | **62.38** | **94.52** | **1.52** | **31.36** | **35.26** | **1.12** |

관찰 :

- **CPU 항목은 전부 ±6% 이내** — 08-23 §6은 "gpu-5 CPU가 공용이라 느림, 개별 CPU에서 개선 기대"였으나 실측 결과 동급
- **GPU 커널은 base 기준 정확히 ~2배** (S1 2.27× / S2 2.05×) — 3060 메모리 대역폭(360GB/s)이 TITAN RTX(672GB/s)의 절반인 것과 일치. 라우팅 커널이 memory-bound라는 방증
- **`S2: host DAG prep + upload`만 패턴 이탈** (1.7~1.9×) — CPU도 GPU 커널도 아닌 H2D 전송 구간. 이 서버의 PCIe/복사 대역폭이 좁은 것으로 추정. opt wall의 14.4%로 병목 순위 상승
- opt S2 GPU route가 1.08×에 그친 것은 tree-center(이전 opt에는 없던) 효과가 하드웨어 열세를 상쇄한 것 — critical path `root=cpu-tree-center`, depth phases 14,389 (base legacy 18,573)

### 감소분 귀속 — 느린 GPU에서 최적화 가치가 커짐

| 최적화 | TITAN (−31.02s 중) | 3060 (−59.26s 중) |
| --- | ---: | ---: |
| incremental vcost/presum (GPU route) | −18.16 s (58.5%) | **−46.84 s (79.0%)** |
| GPU-FLUTE (RSMT) | −7.57 s (24.4%) | −7.49 s (12.6%) |
| GPU batch generation | −5.25 s (16.9%) | −5.00 s (8.4%) |

- CPU 쪽 최적화 절감량은 두 환경에서 **절대값이 거의 동일**, GPU 커널을 지우는 vcost/presum 절감만 2.6배로 확대
- → 배율이 1.99× → 2.68×로 커진 이유. GPU가 느릴수록 GPU 일을 없애는 최적화가 더 값짐

### score (evaluator)

| run | opt | base |
| --- | ---: | ---: |
| 1 | 397,656,564.96 | 397,661,199.37 |
| 2 | 397,657,812.22 | 397,662,412.82 |
| 3 | 397,655,418.98 | 397,662,458.70 |

- 평균 delta **−5,425 (−0.0014%)** — 08-23의 −8,186 (−0.0021%)과 같은 노이즈 수준, 품질 변화 없음
- 런 간 흔들림(opt ±1,200 / base ±630)이 delta와 같은 자릿수 → 개별 런의 delta에 의미 두지 말 것

## 2. `bsg_chip` — 첫 측정 (2회, 이전 환경 기록 없음)

- grid : `L=9 X=1532 Y=2077 cells=28,637,676 tracks=15,968`
- 개별 total : opt 13.12 / 13.12 · base 25.02 / 25.04 — 재현성 사실상 완전

| 항목 | base | opt | 개선 |
| --- | ---: | ---: | ---: |
| input | 1.05 | 1.05 | — |
| build CUDA database | 1.32 | 1.34 | +1.5% |
| S1: RSMT, CPU FLUTE | 4.41 | 1.58 | −64.2% |
| S1: batch generation | 0.79 | 0.41 | −48.1% |
| S1: DAG build (DFS) | 0.36 | 0.46 | +27.8% |
| **S1: GPU route batches** | **10.03** | **4.56** | **−54.5%** |
| S2: host DAG prep + upload | 0.72 | 0.74 | +2.8% |
| **S2: GPU route batches** | **5.63** | **2.04** | **−63.8%** |
| **total** | **25.03** | **13.12** | **−47.6% (1.91×)** |

- base의 `S2: preprocessing`·`S2: batch generation`은 2% 문턱 아래라 other로 접힘 → 해당 행 비교 생략
- S2 critical path : opt `cpu-tree-center` phases 11,926 vs base `legacy` 17,632 (**−32%**)
- group 대비 S1 GPU route 비중이 큼 (opt wall의 34.7%) — 다음 최적화 대상 후보

### score (evaluator)

| run | opt | base |
| --- | ---: | ---: |
| 1 | 112,435,794.21 | 112,474,871.05 |
| 2 | 112,435,766.69 | 112,474,890.59 |

- delta **−39,100 (−0.035%)** — opt가 소폭 좋음 (overflow −37,587이 대부분). 전 런 open 0 · incompleted 0

## 3. `mempool_cluster_ranking` — 12GB 카드의 한계

### opt : 실행 불가 (OOM)

```
GPU-FLUTE allocate score si scratch: out of memory
```

- 2회 시도 모두 같은 지점에서 abort (crash 로그 : `run/logs-0825-rtx3060/mempool_cluster_ranking.opt1.log`)
- grid 206M cells의 DB만으로 7.6GB를 쓰는 상태에서 GPU-FLUTE scratch(고차수 net pin 배열, MAX PINS 456)가 안 들어감
- TITAN RTX 24GB에서만 돌던 구성. 미시도 우회 : `INSTANTGR_GPU_FLUTE_MAX_DEGREE`로 고차수 net을 CPU로 돌리기 ([database_cuda.hpp](../../src/database_cuda.hpp) 실험 knob), 또는 cluster만 `GPU_FLUTE=0`

### base : 완주는 하나 비정상적으로 느림 — 927.45s (1회)

| 항목 | TITAN off | 3060 base | 배율 |
| --- | ---: | ---: | ---: |
| input | 14.30 | 13.07 | 0.91 |
| S1: RSMT, CPU FLUTE | 54.60 | 55.62 | 1.02 |
| S1: batch generation | 16.57 | 16.77 | 1.01 |
| **S1: GPU route batches** | **38.56** | **688.05** | **17.8×** |
| S2: batch generation | 14.03 | 15.29 | 1.09 |
| S2: host DAG prep + upload | 8.57 | 13.30 | 1.55 |
| **S2: GPU route batches** | **51.26** | **100.59** | **1.96×** |
| **total** | **225.28** | **927.45** | **4.12×** |

- CPU 항목 동일, S2 GPU 1.96× — 여기까지는 group과 같은 패턴
- **S1 GPU route만 17.8×로 폭주.** 실행 중 GPU 메모리 11.9/12.3GB — 대역폭 2배 차이로는 설명 안 되는, 메모리 한계 직전 상태의 이상 동작으로 추정. 원인 진단은 nsys 필요 (미실시)
- evaluator : 1,780,911,995.75 — 이전 off 1,780,901,267 대비 +0.0006%, 품질 문제 없음. open 0 · incompleted 0
- **이 디자인의 시간 수치는 이 서버에서 A/B 용도로 쓰지 말 것** — opt는 못 돌고 base는 이상 동작

## 4. 정리

- **재현성** : group 3회·bsg 2회 모두 편차 ±1.4% 이내 — 단독 서버라 이전 환경보다 측정이 깨끗함
- **이 서버에서 유효한 A/B 디자인** : `mempool_group`, `bsg_chip`, (`mempool_tile_rank` — 미기록이나 동작 확인됨). cluster는 제외
- **환경이 바꾼 결론은 없음** — 최적화별 절대 절감량은 이전 환경과 일치. GPU가 느려 상대 배율만 커짐 (1.99×→2.68×)
- 병목 순위 변화 : `S2: host DAG prep + upload`가 opt wall 14.4%로 상승 (PCIe 추정) — DAG 구성·FLUTE와 함께 호스트/전송 쪽이 다음 대상
- [ ] cluster opt를 `GPU_FLUTE_MAX_DEGREE` 캡으로 돌려 12GB에서의 우회 가능 여부 확인
- [ ] cluster base S1 GPU route 17.8× 폭주 원인 (nsys)
- [ ] `mempool_tile_rank` 기록 추가
